# Databricks App Configuration
# Not needed for CLI deployment - kept for reference only
#
# For CLI deployment, use:
#   databricks apps deploy <app-name> --source-code-path /Workspace/apps/feedback_review
#
# The app.yaml file defines the runtime configuration
